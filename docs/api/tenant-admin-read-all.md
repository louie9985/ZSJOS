# 当前租户管理员全量读取实施记录

本记录是实施覆盖表，不代表完整方案已验收。两种管理员身份由 System 有效用户、当前租户及启用角色绑定判定；菜单和操作权限仍独立授权。读取例外不得传递到命令、租户拦截器或通用权限判断。

| 范围 | 已实现的读取边界 | 待完成或待验收 |
| --- | --- | --- |
| System | PermissionApi 能力；认证返回 dataAccess.tenantReadAll；有效身份、角色和租户检查 | 真实角色撤销缓存及模拟身份集成验证 |
| 下属销售 | 岗位人员列表、概览、客资和任务读范围；写操作继续使用原管理范围 | 真实停用销售、筛选、分页及双端验证 |
| 客资与订单 | 主要对象读取、管理范围及敏感身份投影；提交人反馈只读例外 | 逐项核验历史、池、审批关联、导出和附件；个人订单范围 |
| 学员与新媒体 | 学员显式 SELF/ALL/USER；历史服务、关联详情；新媒体主要列表和对象读边界 | 高级筛选最新修改回归；双端个人切换覆盖；关联一致性完整验证 |
| 个人日历 | 显式范围与归属字段；双端切换；更新和删除仍限本人 | 当前工作树外部变动后的重检；浏览器与真实接口 |
| 反馈、表单及敏感读取 | 反馈对象读例外；标记已读拆成 mark-read-own；独立表单提交记录 provider；学员敏感读取保留入口和审计 | 个人列表切换与 UI；附件及无副作用集成验证 |
| 工作计划、工单、提现 | 部分明确读边界；提现完整卡号读取判断 | 个人范围切换、列表与详情一致性、敏感入口和写隔离全面回归 |
| FMS | 非成员管理员可读账套，列表投影为只读成员能力；写入、所有者校验保持原规则 | 账套读校验全部调用点复核及双端真实验证 |
| PMS、EAM | 项目、知识库与内容读取能力；资产范围 | 子资源、操作能力和完整回归 |
| HRM、BPM | 保留现有管理读取与个人操作分离方案 | 尚未完成本轮全面接入与验收 |

验证区分：已运行源码测试和双端类型检查，不能替代真实接口及浏览器证据。最新高级筛选及测试依赖补齐尚需重跑。2026-09-21 18:28 观察到当前工作树在执行期间发生外部暂存、提交及文件替换，暂停源码编辑等待串行写入条件恢复；此前结果不能证明该外部变更后的完整工作树。详见 [工作流记录](../../handoff/main.md)。

无需数据迁移，不新增依赖，不自动授予角色菜单，不修改真实账号。本轮未实施部署或共享服务操作。
