# 开发计划

1. 完成登录、Token、用户信息和动态菜单。
2. 按业务所有权复用现有 system、CRM 和其他业务 API，不建立通用 `/zsjos/workbench/*` 聚合接口。
3. 新增中世健自建业务能力时，在 `yudao-module-zsjos` 提供聚焦接口，并通过 typed service 接入工作台。
4. 建立客户、线索、跟进等员工工作流页面。
5. 补充接口测试、前端组件测试、权限验收、浏览器验收和部署配置。
