# 学员概览 9:3 改版与字段保留

2026-09-18。仅 Workbench 展示调整；不修改后端数据、权限、字典或表单。用户最终确认：阅读态采用短字段两列、长内容通栏的分组网格，标题保留、正文全部展开；编辑态仍为四列。

## 展示边界

- 顶部身份、服务上下文、账号标签及学员/服务工具栏保留；概览按 12 栏 9:3 排列。
- 右侧状态与定位卡操作在上、档案在下，自然滚动。≤960px 单列，状态/档案先于正文。
- 定位卡概览阅读使用混合字段网格和高遮盖度磨砂标题栏。编辑四列表格不变。通用 AttachmentCard 通过 load 回调读取业务签名地址，以彩色 SVG 区分文档、图片、PDF、表格、演示稿、音视频、压缩包及未知文件，不认识业务端点。
- 原模板分组边界和字段顺序保留，无分组不编造业务分类；字段全部展开，阅读态不展示填写提示；编辑态保留模板提示。窄屏字段单列，桌面短字段两列、长内容及附件通栏。模板失去主字段的孤立参考仍可查看。

## 原内容 → 新位置

| 原内容 | 新位置及约束 |
| --- | --- |
| 姓名、头像 | 顶部身份头，完整资料保留表单原始字段 |
| 学员编号、手机、微信 | 右侧基本档案，全值可读 |
| 服务状态、兼职开通状态 | 右侧统一状态区，保留原开通权限、加载和重试 |
| 定位卡状态、次数、责任人、提交时间 | 右侧流程区；正文仍保留版本信息及流程各时间 |
| 学习规划师、编导、职业规划师、运营 | 右侧服务团队，未指派明确显示 |
| 访谈阶段、预约、进度、确认完成度、访谈稿 | 右侧定位访谈信息组；数量改称“访谈记录”，保持原计算含义；原授权访谈入口保留 |
| 课程、规格、班级、开通时间 | 右侧当前服务，来自当前服务响应 |
| 工作单位、岗位、学历、院校、学习目的 | 现有学员信息表普通详情，尊重字段启用、排序、快照及返回遮罩；其余字段在完整资料 |
| 定位卡字段、required、值和字典快照 | 阅读名称与内容同行、短字段两列；编辑保持四列及原始提示；分组与字段值完整保留 |
| referenceFor 参考资料 | 对应字段内部独立区域，不混成无归属素材池 |
| 附件及确认凭证 | 所属字段/版本内文件卡，图标类型、名称、大小、媒体/PDF 预览、下载和失败重试；操作时重新获取有效地址 |
| 运营与学员意见 | 两份独立显示，避免原 `a || b` 遮掉其中一份 |
| 最新已确认版本、历史提交 | 全文原位可查、历史只读；附件与意见保留 |
| 选择主卡、填写、复核、驳回、生成链接、修订、凭证上传 | 继续执行原接口、availableActions/can* 和版本参数；历史不生成修改入口 |

## 权限与验证

真实权限为 `zsjos:student-info-form:read`；计划中省略 `-form` 的拼写不作为新权限。资料还要求 contact context 中的 `student-info` 可见。使用当前服务关联 Lead，按 StudentDetail 既有约定回退学员关联 Lead；不推测、不请求其他记录。服务端继续校验租户与对象可见性。

复用现有测试设施。`test/student-overview.html` 挂载正式页面，HTTP adapter 全部为隔离合成数据，未预期写请求拒绝；`test/student-overview.py` 验证响应状态、版本操作目标、完整字段/参考/附件、原位历史及桌面手机布局。该证据不等于已验证部署环境真实账号权限或真实审核写入。


## 2026-09-18 后续确认：混合字段网格与预览

用户在四列表格验收后确认改为短字段两列、实际长内容/附件/参考通栏的混合网格。正文全部展开；短 textarea 不因类型独占整行。120 字符为长内容呈现阈值，不改变数据。分组保留服务端顺序，阅读字段与标题使用 13px base token 和常规/中等字重；编辑四列不变。

学员与运营意见作为版本内“本轮确认与反馈”放在正文前，两栏排版、全文可读，未知结果不推测。ResourceLink/LinkedText 统一合法 HTTP(S) 和显式内部路由呈现，保留普通文本，参考素材使用局部 Context 启用，不全站替换。AttachmentCard 图片缩略图可点开等比例预览，弹窗提供原文件下载、关闭与错误重试；预览与下载继续使用原授权加载器并刷新地址。


## 平台、阶段与参考素材（最终布局）

四个平台主页搭建按现有 pc_homepage_* 字段及 referenceFor 归属组合，桌面四列；S0 独占一整行，S1–S6 两行三列。以容器实际宽度在 720px 以下降为两列、420px 以下单列，S0 始终全宽。只有实际返回的启用字段参与布局，不创造缺失字段，不跨越其他字段或服务端组边界重排。普通字段按“名称：内容”同行；阅读态隐藏提示，编辑原四列表格不变。

平台参考来自 viral_account，阶段参考来自 viral_content，保存和读取所选素材版本快照。当前仓库模板 S0 未配置参考字段，前端不补造。素材卡读取 coverPreviewUrl，缺失才回退 __cover__ 文件；没有封面显示明确空态。封面可放大，“预览参考内容”读取原定位卡及提交版本的 snapshotMaterial 接口并展示完整素材，支持加载、读取错误及重试。普通文件和确认凭证继续走原附件接口，不能冒充素材库引用。

确认凭证位于各版本的“本轮确认与反馈”内，独立标题“学员确认凭证（运营上传）”；保留上传时间、文件预览和下载。当前操作区仍只对当前定位卡生效。
