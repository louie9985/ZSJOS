# Avatar Storage Fix Workstream

## Active delivery: stable private-bucket employee avatars

- Workstream ID: `avatar-storage-fix`
- Goal: fix Tencent COS virtual-host URL generation and prevent default/personal employee avatars from persisting expiring private-bucket presigned URLs
- Non-goals: migrate non-avatar attachments, change bucket ACLs, delete historical files, rotate credentials, restart shared services, or perform branch/commit/push operations
- Branch: started on `codex/student-contact-review-fixes`; externally changed to `main` before delivery
- Worktree: `D:\ZSJ-OS`
- Base commit: `efb8f7bb0987c66fe290a26605b32e5e474106a9`
- Target branch: current local `main`
- Ownership scope: Infra S3 URL construction and avatar-only file access/upload contracts; System personal/default avatar persistence and projection; Vue Admin and React Workbench avatar upload consumption; the next available MySQL avatar migration and bootstrap/schema/verification wiring; directly affected avatar architecture/API documentation; focused tests; this handoff file
- Owner: Codex `/root`
- Dependencies: existing Infra file clients and file table, System user/profile/auth contracts, current master Tencent COS configuration, existing frontend upload components; no new dependency planned
- Integration order: preserve unrelated worktree changes -> fix and test S3 endpoint construction -> add restricted stable avatar file contract -> persist stable file references with legacy URL fallback -> update frontend flows -> add repeatable migration and documentation -> run backend/frontend/SQL/HTTP verification -> append delivery evidence
- Verification plan: S3 unit tests for Tencent virtual-host and MinIO path-style modes; avatar upload/access service and permission tests; Infra/System module tests and compile; Workbench focused tests/typecheck/build; Admin typecheck/build; migration syntax/repeatability and bootstrap verification review; real local upload plus HTTP 200 check without exposing signed URLs or credentials

## Delivery Entry 2026-08-20 11:40:29 +08:00

- Branch: started on `codex/student-contact-review-fixes`; externally changed during the task and delivered from `main`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `c1017b49f37c4d262e269658d04a396754c7e91a` (no commit created; initial base was `efb8f7bb0987c66fe290a26605b32e5e474106a9`)
- User goal: repair default-avatar upload previews and prevent default/personal employee avatars from repeatedly becoming inaccessible after storage SQL or private URL expiry
- Key decisions: fixed S3 virtual-host domain derivation instead of requiring a non-empty Tencent domain workaround; retained `system_users.avatar` and `zsjos.user.default-avatar` as URL sources of truth; added an avatar-only upload/read boundary that persists a stable backend URL containing `infra_file.id`; kept the bucket private; restricted stable reads to `system/user/avatar` and `employee/avatar` JPG/PNG/WebP records; used content hashes for immutable avatar object names; did not add a schema migration or alter non-avatar uploads
- Execution or analysis result: new avatar uploads no longer persist 24-hour private-bucket presigned URLs; the stable reader uses each file's original storage configuration; Vue Admin user/default-avatar uploads and Workbench profile uploads now use the dedicated endpoint; the current local Tencent master `domain` was conditionally restored for immediate compatibility; no test upload object was created because the backend was not listening
- Changed files: `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/controller/admin/file/FileController.java`; `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/enums/ErrorCodeConstants.java`; `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/framework/file/core/client/s3/S3FileClient.java`; `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/service/file/FileService.java`; `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/service/file/FileServiceImpl.java`; `backend/yudao-module-infra/src/test/java/cn/iocoder/yudao/module/infra/framework/file/core/s3/S3FileClientTest.java`; `backend/yudao-module-infra/src/test/java/cn/iocoder/yudao/module/infra/service/file/FileServiceImplTest.java`; `frontend/admin/src/api/infra/file/index.ts`; `frontend/admin/src/components/UploadFile/src/UploadImg.vue`; `frontend/admin/src/components/UploadFile/src/useUpload.ts`; `frontend/admin/src/views/system/user/DefaultUserAvatarForm.vue`; `frontend/admin/src/views/system/user/UserForm.vue`; `frontend/workbench/src/services/api.ts`; `frontend/workbench/docs/api-contract.md`; `docs/architecture/data-and-permission-flow.md`; and this handoff file
- Verification evidence: focused Infra `S3FileClientTest,FileServiceImplTest` passed with 38 executed, 0 failed, 6 external cloud tests skipped; Workbench 54 files/317 tests passed; Workbench typecheck and production build passed; Vue Admin typecheck and local production build passed with an existing legacy CSS minifier warning; scoped `git diff --check` passed with line-ending notices only; database precondition check reports the current Tencent master domain `PASS` and confirms the failed live-upload attempt created zero matching file rows; full Infra suite executed 209 tests with all avatar tests passing but failed on the unrelated pre-existing `CodegenEngineUniappTest.testExecute_treeSearch`, which also failed when rerun alone
- Dependency or integration impact: no new dependency, schema migration, bucket ACL change, file deletion, branch operation by this workstream, commit, push, or publication. One confirmed external-state change updated only `infra_file_config.id=24` `domain` after validating its master/storage/endpoint/current-empty preconditions. Three existing System users still have legacy signed avatar URLs; they retain the documented fallback until replaced by a new stable upload.
- Remaining work: obtain separate confirmation to start the local backend on port `48080`, deploy the changed Infra code, perform one controlled real avatar upload/read check, and verify the default-avatar preview in the browser. Rotate storage credentials exposed by historical repository backups as a separate security operation.

## Delivery Entry 2026-08-20 11:50:42 +08:00

- Branch: `main`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `c1017b49f37c4d262e269658d04a396754c7e91a` (no commit created)
- User goal: confirm and continue the default/personal avatar repair through build and runtime verification
- Key decisions: preserved the private bucket and stable avatar-reader design; corrected stable URL generation to include the framework-configured Admin API prefix instead of assuming the controller mapping already contained it; added a focused controller contract test; did not stop or restart the running backend without the separately required confirmation
- Execution or analysis result: repository review found that `ServletUriComponentsBuilder.fromCurrentContextPath()` excluded Yudao's dynamically applied `/admin-api` controller prefix, which would reproduce the reported upload-success/preview-failure symptom; the returned URL now uses `WebProperties.adminApi.prefix` and resolves to the actual public avatar route
- Changed files: `backend/yudao-module-infra/src/main/java/cn/iocoder/yudao/module/infra/controller/admin/file/FileController.java`; `backend/yudao-module-infra/src/test/java/cn/iocoder/yudao/module/infra/controller/admin/file/FileControllerTest.java`; and this handoff file
- Verification evidence: focused `S3FileClientTest,FileServiceImplTest,FileControllerTest` run passed with 39 tests executed, 0 failures, and 6 disabled external-cloud tests; the controller test asserts an absolute `https://<host>/admin-api/infra/file/avatar/42` result; all 28 reactor modules compile and a skip-repackage install passed; executable repackaging remains blocked only because Windows PID `27700` holds the current `yudao-server.jar` while listening on port `48080`; scoped `git diff --check` passed with a line-ending notice only
- Dependency or integration impact: no new dependency, schema migration, bucket ACL change, file deletion, branch operation, commit, push, or publication; the local Maven repository received the current reactor artifacts during build verification
- Remaining work: after explicit confirmation, stop PID `27700` (brief local API outage on port `48080`), build the executable server jar, restart it against the current local MySQL/Redis configuration, perform one controlled avatar upload/read check, and verify the Admin default-avatar preview in the browser
