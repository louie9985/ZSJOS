# Workstream Handoff: crm-lifecycle-gap-implementation

- Workstream ID: `crm-lifecycle-gap-implementation`
- Status: `active`
- Goal: Implement the confirmed CRM lead-to-deal lifecycle gaps and the approved realtime notification, duplicate-resolution, authentication, lead-filter, and employee-avatar repairs without changing unrelated existing behavior.
- Non-goals: Cashback withdrawal or payment, export, maintenance mode, WeCom, email, customer merge, standalone customer management, full opportunity management, refund, void, or cancellation workflows; unrelated notification scenes, report statistics, pagination totals, external service control, and unresolved business rules explicitly deferred to a later discussion.
- Branch: `codex/crm-lifecycle-gap-implementation`
- Worktree: `D:\ZSJ-OS`
- Base commit: `9e8cdb498278b93813941f3210ca4f4c1b98f306`
- Target branch: `main`
- Ownership scope: Confirmed lifecycle gaps in `backend/yudao-module-zsjos`; tenant-scoped Workbench behavior configuration; approved System notification/account public surfaces; lead assignment, duplicate resolution, filter-profile, authentication and employee-avatar behavior; corresponding Admin and Workbench screens; MySQL migrations and bootstrap artifacts; focused tests; and directly affected architecture/API/navigation/deployment documentation.
- Owner: Codex CRM lifecycle workstream
- Dependencies: Existing Yudao System, BPM, Infra file, tenant, permission, dictionary, and WebSocket public facilities; no new dependency is planned.
- Integration order: Domain and schema compatibility, duplicate review and submission, public-pool collaboration and transfer, order/cashback/approval concurrency, notification reliability and account validation, tenant behavior settings, filter/auth/avatar frontend repairs and documentation, then integrated verification.
- Verification plan: Focused backend unit, permission, state-transition, notification-routing and concurrency tests; API contract and idempotency checks; MySQL migration/static/repeatability checks; Admin and Workbench unit/lint/type checks and production builds; desktop/mobile browser checks for notifications, login routing, filters and avatars; real local requests when an already-running compatible environment is available. Starting, stopping, or reconfiguring shared services remains separately controlled.

## Entries

### 2026-08-14 17:22 Asia/Shanghai

- Branch: `codex/crm-lifecycle-gap-implementation`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `9e8cdb498278b93813941f3210ca4f4c1b98f306` (changes remain uncommitted by request)
- User goal: Implement the confirmed CRM lifecycle gaps on a new branch without affecting existing business, including the confirmed order notification matrix and account/submission/order rules.
- Key decisions: Submission notifications are split between approval reviewers and the lead submitter; intermediate BPM approve/add-sign/transfer actions and cancellation produce no business notification; rejection notifies the formal sales and current round submitter; final approval notifies sales actors and the lead submitter. Sales self-sourced leads may select one optional new-media provider. All orders require 1-6 vouchers, including zero-amount orders. Notification events now enter a durable per-rule outbox with claim leases and 1/5/30-second retries. Username uniqueness is tenant-local and binary-collated; non-empty mobile uniqueness uses a generated column.
- Execution result: Completed the order notification call-chain, self-sourced provider API/UI, voucher validation/UI limits, notification outbox persistence/worker/retention, V056 schema migration, baseline synchronization, duplicate/transfer notification scenes, and related account/session behavior already present in the workstream.
- Changed files: System notification API, outbox DO/mapper/service and focused test; ZSJOS lead/order/cashback services, VOs, mappers and notification providers; Admin user form; Workbench lead submission, order entry, profile, API and validation; `script/sql/mysql/migrations/V056__crm_lifecycle_confirmed_rules.sql`; `script/sql/mysql/schema/core.sql`; `script/sql/mysql/00-bootstrap-schema.sql`; this handoff file.
- Verification evidence: `mvn -f backend/pom.xml -pl yudao-module-system,yudao-module-zsjos -am -DskipTests compile` passed. Workbench `npm run typecheck` and `npm run build` passed. `git diff --check` passed. `./zsjos-db.ps1 check` passed. Full reactor tests were not green because pre-existing `yudao-module-infra` `CodegenEngineUniappTest.testExecute_treeSearch` failed; direct ZSJOS tests also exposed existing dependency/classpath and broad Mockito-session failures, so they are not accepted as a clean regression signal.
- Dependency/integration impact: No new Maven/npm dependency. Runtime depends on existing Yudao tenant, notification, WebSocket, BPM and scheduler facilities. V056 is additive and repeatable; no business data deletion or migration execution was performed.
- Remaining work: Real MySQL execution/repeatability, BPM callbacks, notification rule/template delivery, WebSocket delivery, browser interaction at desktop/mobile widths, and full test-suite stabilization remain unverified. The admin full type check still has existing unrelated type errors, including the touched user form's pre-existing post type mismatch.

### 2026-08-14 18:30 Asia/Shanghai

- Branch: `codex/crm-lifecycle-gap-implementation`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `9e8cdb498278b93813941f3210ca4f4c1b98f306` (all lifecycle changes remain uncommitted by request)
- User goal: Complete the approved CRM lifecycle repair plan on the current branch, including database and Outbox reliability, lead identity/transfer behavior, repurchase/cashback/BPM concurrency, account validation, frontend flows, tests, and documentation.
- Key decisions: Reliable Outbox delivery remains limited to `in_app`; escaped worker exceptions retry only for Spring-classified transient data-access failures. Historical submitter rights require an enabled account and active corresponding partner subject without department/post requirements. Disabled partners do not receive new cashback and do not block order creation. Personal-profile mobile validation now uses the same backend mobile-format validator as account management. No branch/worktree operation, dependency addition, commit, merge, push, service start, or database execution was performed.
- Execution result: Implemented and reviewed the V056/baseline account and Outbox schema, claim-token delivery ownership, tenant-aware publication, notification retention and recipient idempotency; lead provider/self-source identity, deep activation snapshots and structured transfer invalidation; repurchase isolation, order number sequencing, voucher enforcement, cashback lifecycle, BPM business termination and lock order; account token revocation and frontend account/order flows. Final review corrected personal mobile validation, partner-state eligibility, Outbox exception retry classification, and the transfer test fixture.
- Changed files: BPM public process API; System notification, account/profile, OAuth token, Outbox DO/mapper/service and tests; ZSJOS lead, partner, duplicate review, transfer, order, cashback, personnel services/VOs/mappers/tests; Admin and Workbench account, submission, voucher, order-detail and API files; V056, bootstrap/schema/verification SQL; notification/order/state-machine/migration documentation; this handoff file.
- Verification evidence: Joint System/BPM/ZSJOS dependency compile passed. System focused tests passed 6/6. ZSJOS focused identity/transfer/order tests passed 24/24. Workbench passed 31 files and 165 tests, typecheck, and production build. Admin production build passed with existing CSS/environment warnings. `zsjos-db.ps1 check` passed; SQL baselines are SHA-256 identical; `git diff --check` passed. Prior browser checks covered the running Workbench at 1440x900 and 390x844 without horizontal overflow.
- Dependency/integration impact: No new Maven/npm dependency. V056 is additive and records its schema versions; runtime uses existing System notification/WebSocket, Infra file, tenant, Redis, and BPM facilities. Integration remains pending because no commit or merge was authorized.
- Remaining work: Admin full `pnpm ts:check` remains blocked by 14 existing repository type errors; the touched `UserForm.vue` error is the pre-existing `PostSimpleVO`/`PostVO` mismatch and the new external-repurchase component is not in the failure list. Real MySQL fresh bootstrap and double V056 execution, real lifecycle/BPM/Outbox/WebSocket requests, and permission-gated interactive checks for external repurchase and terminated-order resubmission remain unverified because no compatible database/account/service operation was authorized or available.

### 2026-08-14 21:04 Asia/Shanghai

- Branch: `codex/crm-lifecycle-gap-implementation`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `9e8cdb498278b93813941f3210ca4f4c1b98f306` (all changes remain uncommitted; no branch, merge, push, or publication operation was performed)
- User goal: Implement the eight confirmed repairs for configurable WebSocket popup duration, duplicate pending-lead delivery, automatic duplicate handling, count-free lead filters, profile avatar persistence feedback, Admin avatar background, immediate auth-expiry logout, and suppression of assignment/acceptance/claim business messages.
- Key decisions: Tenant runtime settings reuse `zsjos_lead_follow_up_rule`; popup duration defaults to five minutes and accepts 1-30 minutes, while the forced pending-assignment modal is unaffected. Automatic duplicate resolution defaults off; when enabled, it selects the newest historical lead by submission time then ID, keeps active/won leads unchanged, reactivates invalid/closed leads, preserves only an eligible owner, and otherwise enters the public claim pool. Assignment realtime events remain functional, but `ASSIGNED`, `REASSIGNED`, `ACCEPTED`, and `CLAIMED` no longer publish business-notification messages. Avatar upload is an immediate preview whose persistence is explicitly deferred until profile save.
- Execution result: Added the tenant runtime-setting API and configuration fields; removed the duplicate business-message route while retaining the assignment realtime event; removed filter-profile counts and their related count queries; implemented configurable automatic duplicate review; made auth refresh failure clear session state and immediately unmount the workbench; corrected profile and Admin avatar behavior; added repeatable V057 schema support and synchronized contracts, architecture, authentication, notification, and migration documentation.
- Changed files: ZSJOS follow-up-rule controller/VO/DO/service, lead dispatch, duplicate review, submission, management and aging-pool services and focused tests; Workbench notification provider, API/session root, configuration, lead-filter and profile pages and documentation; Admin user-list avatar rendering; `script/sql/mysql/migrations/V057__lead_runtime_settings.sql`, bootstrap/schema/verification SQL and migration documentation; directly affected lead-submission, notification, permission-flow and authentication documents; this handoff file.
- Verification evidence: Clean reactor main compilation rebuilt 479 ZSJOS source files; reactor test compilation passed; focused backend suite passed 49 tests with zero failures/errors. Workbench passed 31 test files/165 tests, typecheck, and production build. Admin production build and focused ESLint for `src/views/system/user/index.vue` passed. `zsjos-db.ps1 check` passed; both SQL baselines have SHA-256 `80DABF2B5843F997FFF18A4EE4F0155A8C206FB2C9B1D1EF6AED556922D46D87`. Desktop and mobile login-page browser checks passed without overlap or horizontal overflow. Final `git diff --check` reported only line-ending warnings, and the Workbench preview remains available at `http://127.0.0.1:4173/`.
- Dependency/integration impact: No Maven or npm dependency was added. V057 is additive and repeatable; no real migration or database mutation was executed. Runtime continues to use existing Yudao tenant, authentication, notification, and WebSocket facilities.
- Remaining work: Admin full `pnpm run ts:check` still reports 14 pre-existing repository errors, including the existing `UserForm.vue` `PostSimpleVO[]`/`PostVO[]` mismatch; focused production build and lint are green. Real MySQL V057 execution, authenticated settings/profile flows, live auth expiry, and end-to-end WebSocket delivery were not run because no compatible authorized backend, account, or database operation was available.

### 2026-08-15 10:30 Asia/Shanghai

- Branch: `codex/crm-lifecycle-gap-implementation`
- Worktree: `D:\ZSJ-OS`
- HEAD commit: `9e8cdb498278b93813941f3210ca4f4c1b98f306` (all changes remain uncommitted; no branch, commit, merge, push, service-control, or database-mutation operation was performed)
- User goal: Implement the confirmed CRM repair plan and close the review-confirmed concurrency and regression gaps around automatic duplicate resolution, transfer approval, follow-up-rule configuration, authentication refresh, avatars, provider data, and conditional form state.
- Key decisions: Automatic resolution locks the review then the matched Lead; a pending-assignee is intentionally not a duplicate-reminder recipient. Public-pool reactivation has no ownership start timestamp. Approved transfer remains valid only while the request's original owner snapshot still matches. Follow-up-rule updates require optimistic `version` matching and return stable error `1_900_003_079` on conflict. Stale refresh and avatar-upload results cannot mutate a newer session or preview. Both Workbench and Vue Admin preserve all follow-up-rule fields and submit the versioned contract.
- Execution result: Completed the eight requested repairs and review follow-ups, including source-user fallback for self-sourced leads, masked provider mobiles and batch department lookup, removal of filter-profile status counts and dead code, async unmount guards, Admin avatar fallback, conditional provider-field reset, restored configuration columns, and directly affected contract documentation. Added focused authentication/avatar tests and completed the Admin follow-up-rule contract that was missing from the initial pass.
- Changed files: ZSJOS lead follow-up-rule, duplicate-review, dispatch/transfer, submission, management services, VOs, mapper, error codes and focused tests; Workbench notification, authentication API/root, configuration, submission, profile and tests; Admin user list and follow-up-rule API/page; V057/bootstrap/schema verification artifacts; lead-submission, authentication, API, permission-flow, notification and migration documentation; this handoff file. Other pre-existing lifecycle workstream changes were preserved.
- Verification evidence: ZSJOS focused backend suite passed 57/57; `yudao-server` full dependency graph `compile` passed. Workbench passed 32 test files/168 tests, typecheck and production build. Admin focused ESLint and production build passed. Admin full `pnpm ts:check` retains the same 14 pre-existing errors and no new error in the follow-up-rule or user-list files. `zsjos-db.ps1 check` passed. Desktop 1440x900 and mobile 390x844 browser checks passed for the available login surface with no overlap or horizontal overflow. `git diff --check` passed with line-ending warnings only. Open Code Review connectivity passed, but the workspace review exceeded the 10-minute command limit and produced no review result, so it is not counted as a successful review.
- Dependency/integration impact: No Maven/npm dependency was added. V057 remains additive and repeatable; no real migration was executed. The full server `package` reached Spring Boot repackaging but could not rename the currently locked `yudao-server.jar`; the non-mutating full dependency compile passed instead, and no running Java service was stopped.
- Remaining work: Authenticated browser checks for the settings table/modal, profile save, Admin avatar fallback, live logout/auth-expiry generation, and WebSocket popup duration/deduplication require a compatible test account and live backend. Real MySQL V057 fresh/double execution and end-to-end assignment/duplicate/transfer requests remain unverified. Full server packaging should be rerun after the process holding `backend/yudao-server/target/yudao-server.jar` is intentionally stopped by an authorized operator.
